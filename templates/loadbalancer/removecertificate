# loadbalancer/removecertificate

cat << EOF
{
    "loadbalancerid": "$loadbalancerid",
    "certificatename": "$certificatename"
}
EOF

# No optional arguments for this request.

# Description:
# Removes ssl certificate from loadbalancer instance.
