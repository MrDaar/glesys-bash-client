# loadbalancer/removefromblacklist

cat << EOF
{
    "loadbalancerid": "$loadbalancerid",
    "prefix": "$prefix"
}
EOF

# No optional arguments for this request.

# Description:
# Removed an ip or prefix from loadbalancer blacklist
