# loadbalancer/disabletarget

cat << EOF
{
    "loadbalancerid": "$loadbalancerid",
    "backendname": "$backendname",
    "targetname": "$targetname"
}
EOF

# No optional arguments for this request.

# Description:
# ables specified target
# A disabled target will NOT recive any traffic
