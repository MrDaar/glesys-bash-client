# loadbalancer/addtoblacklist

cat << EOF
{
    "loadbalancerid": "$loadbalancerid",
    "prefix": "$prefix"
}
EOF

# No optional arguments for this request.

# Description:
# Blocks ip or prefix from access any url on the loadbalancer
