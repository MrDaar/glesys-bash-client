# loadbalancer/destroy

cat << EOF
{
    "loadbalancerid": "$loadbalancerid",
    "keepip": "$keepip"
}
EOF

# No optional arguments for this request.

# Description:
# Completly destroys a specified loadbalancer instance
