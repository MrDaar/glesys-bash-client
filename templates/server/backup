# server/backup

cat << EOF
{
    "serverid": "$serverid",
    "enabled": "$enabled"
}
EOF

# No optional arguments for this request.

# Description:
# Enable or disable backup for a server
# Currently only supported for VMWare servers.
