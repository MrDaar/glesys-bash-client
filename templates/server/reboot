# server/reboot

cat << EOF
{
    "serverid": "$serverid"
}
EOF

# Optional arguments:
# "transactionstarttime": "$transactionstarttime",

# Description:
# Reboots a server
# Does the same thing as server/stop with type set to reboot.
