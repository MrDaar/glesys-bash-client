# ip/listfree

cat << EOF
{
    "ipversion": "$ipversion",
    "datacenter": "$datacenter",
    "platform": "$platform"
}
EOF

# No optional arguments for this request.

# Description:
# Get a list of all ip adresses that are
