# paymentcard/payinvoices

cat << EOF
{
    "invoicenumbers": "$invoicenumbers",
    "paymentcardid": "$paymentcardid"
}
EOF

# No optional arguments for this request.

# Description:
# Pay one (or more) invoices with an payment card
