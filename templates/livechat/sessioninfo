# livechat/sessioninfo

cat << EOF
{
    "sessionid": "$sessionid",
    "sessionkey": "$sessionkey"
}
EOF

# No optional arguments for this request.

# Description:
# This function can be used to know if the current session is active or not.
