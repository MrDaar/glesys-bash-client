# user/register

cat << EOF
{
    "name": "$name",
    "username": "$username",
    "password": "$password"
}
EOF

# Optional arguments:
# "personalid": "$personalid",

# Description:
# Register a new user.
