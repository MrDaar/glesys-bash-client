# user/login

cat << EOF
{
    "username": "$username",
    "password": "$password"
}
EOF

# Optional arguments:
# "otp": "$otp",
# "ip": "$ip",

# Description:
# This function is used to login with an username and password.
