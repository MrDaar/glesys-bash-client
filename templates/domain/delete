# domain/delete

cat << EOF
{
    "domainname": "$domainname"
}
EOF

# Optional arguments:
# "forcedeleteemail": "$forcedeleteemail",

# Description:
# letes a domain from the dns system
# By default, you will get an error message if
