# domain/deleterecord

cat << EOF
{
    "recordid": "$recordid"
}
EOF

# No optional arguments for this request.

# Description:
# Removes a dns record from a domain
# Get the recordid from domain/listrecords
